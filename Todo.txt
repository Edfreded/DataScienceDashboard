Dashboard:
    navbar styling
    plotly styling
    Filter - currently done in plotly, styling is hardcoded
    background ui design
    Theme Selector

Background:
    Plotly easier theme settup. currently hardcoded
    Css cleanup, lots of bloat styling (see shiny navbar commit)
    Css modules, currently everything is in one file
    Hot reload fix
    Speed/ optimization
    Actual dataset use